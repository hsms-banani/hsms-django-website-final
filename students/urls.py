# students/urls.py 
from django.urls import path

app_name = 'students'

# Empty urlpatterns since we're only using API routes
urlpatterns = [
    # Add any frontend view URLs here if needed
    # For now, keeping this empty since we're only using API endpoints
]